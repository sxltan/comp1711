#include <stdio.h>

typedef struct
{
    char date[20];
    float bloodIron;
} reading;

int main() {

    // Create a single instance
    reading today;

    // Create an array with 7 members to store a weeks worth of readings
    reading week[7];

}